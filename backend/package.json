{
  "name": "tuesday-backend",
  "version": "1.2.0",
  "description": "Tuesday - Self-hosted project management tool backend",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify src/index.ts --outfile tuesday",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "search:backfill-docs": "bun run src/scripts/backfill-doc-search.ts",
    "cleanup:hiring-doc-orphans": "bun run src/scripts/cleanup-orphaned-hiring-docs.ts",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.6.14",
    "nodemailer": "^8.0.1",
    "openai": "^6.18.0",
    "postgres": "^3.4.5",
    "yjs": "^13.6.29",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.10.0",
    "@types/nodemailer": "^7.0.11",
    "bun-types": "latest",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.3.3"
  }
}
